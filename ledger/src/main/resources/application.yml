server:
  port: 8082
spring:
  application:
    name: ledger-service
  datasource:
    url: jdbc:postgresql://localhost:5434/ledger
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true

relicarium:
  security:
    jwt:
      secret: relicarium-dev-jwt-secret-min-32-characters-long
      issuer: relicarium
      access-token-ttl: PT8H