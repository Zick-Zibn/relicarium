server:
  port: 8083
spring:
  application:
    name: auction-service
  datasource:
    url: jdbc:postgresql://localhost:5435/auction
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true