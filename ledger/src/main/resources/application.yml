server:
  port: 8082
spring:
  application:
    name: ledger-service
  datasource:
    url: jdbc:postgresql://localhost:5434/ledger
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true